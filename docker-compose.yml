services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    restart: always
    networks:
          - my-net
  frontend:
    build: ./frontend
    ports:
      - "8080:80" 
    restart: always
    networks:
          - my-net

networks:
  my-net:
    driver: bridge